include:
  - project: graphaware/devops/gitlab-ci-templates
    file: neo4j-framework-template.yml

variables:
  JAR_NAME: neo4j-to-elasticsearch

publishJavaDoc:
  extends: .publishJavaDoc
  variables:
    FROM: target/apidocs
    TO: neo4j-to-elasticsearch